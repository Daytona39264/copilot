# Environment Variables Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to git (it's in .gitignore)

# Anthropic API Configuration (optional)
# Required for AI-powered features
# Get your key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Application Configuration
# Set to "development" or "production"
ENVIRONMENT=development

# Server Configuration
# Host and port for the FastAPI server
HOST=0.0.0.0
PORT=8000

# Database Configuration (if needed)
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname

# GitHub Token (if needed for API access)
# For Personal Access Tokens:
# - Fine-grained tokens (recommended): https://github.com/settings/personal-access-tokens/new
# - Classic tokens: https://github.com/settings/tokens
# Use fine-grained tokens with minimal scopes when possible
# GITHUB_TOKEN=your_github_token_here

# Logging
LOG_LEVEL=INFO
